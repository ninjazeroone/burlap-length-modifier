# burlap-length-modifier

Simple Burp Suite plugin to calculate the length of Burlap protocol's message length and overwrite the first couple of bytes with it just before the sending of modified message.
